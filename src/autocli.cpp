#include "TinyUtil.h"
#include <memory>

using namespace std;
using namespace wfan;

/*
 connect server and send a packet
 */

using namespace wfan;
int main(int argc, char *argv[])
{
    //create a PDU and send it to 
    return ret;
}
